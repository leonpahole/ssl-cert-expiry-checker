# SSL cert expiry checker

## Usage

```
usage: check.py [-h] [-l L] [-f F]

optional arguments:
  -h, --help  show this help message and exit
  -l L        Comma separated list of domains
  -f F        Path to file with domains listed (comma, space or new line separated)
```
